// Package index — производный поисковый индекс поверх vault: схема SQLite,
// полный и инкрементальный скан, парсер языка запросов из SPEC §8.5.
//
// Индекс всегда вторичен по отношению к файлам. Разошёлся с диском — он
// перестраивается, а не файлы правятся под него.
//
// В базу одновременно пишут приложение и tasker-mcp, поэтому WAL и busy_timeout
// обязательны, а запись сериализуется на нашей стороне.
//
// Пакет не импортирует Wails — см. CLAUDE.md, инвариант 4.
package index
