// Package vault отвечает за заметки на диске: чтение и запись .md, разбор и
// сборку frontmatter, генерацию slug и ULID, корзину и вложения.
//
// Это нижний слой: он знает про файлы и ничего не знает про SQLite, про окно и
// про то, кто его вызвал — приложение или tasker-mcp.
//
// Инвариант: запись только атомарная (временный файл рядом → fsync → rename), и
// неизвестные поля frontmatter переживают перезапись. См. SPEC §4.2.
//
// Пакет не импортирует Wails — см. CLAUDE.md, инвариант 4.
package vault
