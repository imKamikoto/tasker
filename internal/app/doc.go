// Package app — сервисы Wails: единственное место в internal/, которое знает про
// оболочку. Здесь живут тонкие обёртки над vault, index, watcher и gitstore,
// пригодные для генерации биндингов, и рассылка событий tasker:* в UI.
//
// Бизнес-правил здесь нет. Понадобилась логика — она пишется слоем ниже, иначе
// её нельзя переиспользовать в tasker-mcp и нельзя протестировать без окна.
//
// Сервисы Wails — синглтоны, и вызовы из вебвью приходят конкурентно: никто их
// не сериализует за нас. См. CLAUDE.md, раздел «Go: стиль».
package app
