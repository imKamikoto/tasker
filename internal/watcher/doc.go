// Package watcher замечает изменения в vault, сделанные не нами: правку файла
// внешним редактором, заметку, созданную tasker-mcp при закрытом приложении.
//
// Watcher — оптимизация задержки, а не источник правды: периодическая сверка по
// mtime ловит всё, что он проспал. На macOS под fsnotify лежит kqueue, поэтому
// следим только за папками и обходим дерево сами. См. SPEC §5.3.
//
// Пакет не импортирует Wails — см. CLAUDE.md, инвариант 4.
package watcher
