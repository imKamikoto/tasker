// Package tasker существует ради одной строки: go:embed не умеет выходить за
// пределы каталога своего пакета, а собранный фронтенд лежит в корне
// репозитория, тогда как точка входа приложения — в cmd/tasker.
//
// Больше здесь ничего быть не должно.
package tasker

import "embed"

// Assets — собранный фронтенд, вшитый в бинарник.
//
// В релизной сборке приложение отдаёт эти файлы вебвью по внутреннему
// протоколу: ни сервера, ни порта, ни сети (docs/DESKTOP.md §3).
//
//go:embed all:frontend/dist
var Assets embed.FS
