// Command tasker — само приложение: окно macOS с WKWebView внутри и сервисы
// Wails поверх internal/*. См. docs/SPEC.md §6.
package main

import (
	"fmt"
	"os"
)

func main() {
	// Фаза 3. См. docs/ROADMAP.md.
	fmt.Fprintln(os.Stderr, "tasker: не реализовано, фаза 3")
	os.Exit(1)
}
